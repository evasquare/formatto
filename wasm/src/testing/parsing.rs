mod get_sections {
    mod code_blocks;
    mod contents;
    mod general;
    mod properties;

    mod headings {
        mod alternative_headings;
        mod hash_headings;
    }
}

mod headings {
    mod alternative_headings;
    mod get_top_heading_level;
}
